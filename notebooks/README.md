# Notebooks

`Dialect_Fusion_Ctg.ipynb` is the original Colab research notebook this
package was refactored from. It is kept here for provenance/reference —
all logic now lives in `src/dialect_fusion/` and is driven via `scripts/`.
